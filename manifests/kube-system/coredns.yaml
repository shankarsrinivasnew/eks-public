---
apiVersion: crd.projectcalico.org/v1
kind: NetworkPolicy
metadata:
  name: coredns
  namespace: kube-system
spec:
  selector: k8s-app == 'kube-dns'
  types:
    - Ingress
    - Egress
  ingress:
    - metadata:
        annotations:
          from: all
          to: dns
      action: Allow
      protocol: TCP
      source:
        selector: all()
      destination:
        ports:
          - 53
    - metadata:
        annotations:
          from: all
          to: dns
      action: Allow
      protocol: UDP
      source:
        selector: all()
      destination:
        ports:
          - 53
  egress:
    - metadata:
        annotations:
          from: dns
          to: dns
      action: Allow
      protocol: TCP
      destination:
        nets:
          - 10.161.160.2/32
          - 10.161.192.2/32
          - 10.161.224.2/32
        ports:
          - 53
    - metadata:
        annotations:
          from: dns
          to: dns
      action: Allow
      protocol: UDP
      destination:
        nets:
          - 10.161.160.2/32
          - 10.161.192.2/32
          - 10.161.224.2/32
        ports:
          - 53
